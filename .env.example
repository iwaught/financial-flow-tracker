# Supabase Configuration
# Get these values from your Supabase project settings
# https://app.supabase.com/project/_/settings/api

VITE_SUPABASE_URL=your-project-url.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Note: The anon key is safe to expose in the frontend
# as it only allows access through Row Level Security policies

# Billing Configuration (US-FFT-016)
# Set to 'true' to enable billing features, 'false' to disable
VITE_BILLING_ENABLED=false

# Stripe Configuration
# Get these from https://dashboard.stripe.com/apikeys
# Use test keys for development (they start with pk_test_)
VITE_STRIPE_PUBLIC_KEY=your-stripe-publishable-key

# Note: NEVER expose your Stripe secret key in the frontend!
# Secret keys should only be used in backend/server environments
